mutation UpdatePersonByName($name: String!, $personAttributes: PersonAttributes!) {
  updatePersonByName(lastName: $name, personAttributes: $personAttributes) {
    firstName
    lastName
    age
  }
}
